# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
warnings_are_errors: true
sudo: required
cache: packages

matrix:
    include:
        - os: osx
          r: release

notifications:
  email:
    on_success: change
    on_failure: change
